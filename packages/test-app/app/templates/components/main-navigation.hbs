<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
  <LinkTo @route="protected" class="navbar-brand">
    Home
  </LinkTo>
  <ul class="navbar-nav mr-auto">
    <LinkTo @route="protected.one" class="nav-item" @tagName="li" data-test-button"one">
      <a class="nav-link" href="#">
        Protected one
      </a>
    </LinkTo>
    <LinkTo @route="protected.two" class="nav-item" @tagName="li">
      <a class="nav-link" href="#">
        Protected two
      </a>
    </LinkTo>
  </ul>
  <form class="form-inline">
    {{#if session.isAuthenticated}}
      <a id="logout-button" {{on "click" this.logout}} class="btn btn-danger" href="#" role="button">Logout</a>
    {{else}}
      {{!-- <a {{action onLogin}} class="btn btn-success" href="#" role="button">Login</a> --}}
    {{/if}}
  </form>
</nav>
